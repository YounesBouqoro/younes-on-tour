rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tours/{tourId} {
      allow read: if resource.data.published == true || (request.auth != null && request.auth.token.email == "younesbouqoro@gmail.com");
      allow create, update, delete: if request.auth != null && request.auth.token.email == "younesbouqoro@gmail.com";
    }
  }
}
